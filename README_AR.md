# 🛍️ منصة التجارة الإلكترونية - الخادم الخلفي

واجهة برمجية (API) جاهزة للإنتاج مبنية بـ Node.js و Express و MongoDB. يوضح هذا المشروع أنماط معمارية على مستوى المؤسسة وأفضل الممارسات وقرارات التصميم لنظام خلفي قابل للتوسع.

---

## 📋 جدول المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المعمارية](#المعمارية)
- [المكدس التقني](#المكدس-التقني)
- [هيكل المشروع](#هيكل-المشروع)
- [التثبيت والإعداد](#التثبيت-والإعداد)
- [تدفق الطلب](#تدفق-الطلب)
- [نقاط نهاية الـ API](#نقاط-نهاية-الـ-api)
- [المصادقة والتفويض](#المصادقة-والتفويض)
- [قرارات التصميم](#قرارات-التصميم)
- [الميزات الرئيسية](#الميزات-الرئيسية)
- [مخطط قاعدة البيانات](#مخطط-قاعدة-البيانات)
- [معالجة الأخطاء](#معالجة-الأخطاء)
- [متغيرات البيئة](#متغيرات-البيئة)

---

## 📊 نظرة عامة

توفر هذه منصة التجارة الإلكترونية:

- **إدارة المستخدمين**: التسجيل والمصادقة وإدارة الملف الشخصي
- **كتالوج المنتجات**: منتجات منظمة حسب الفئات مع البحث والتصفية
- **سلة التسوق**: إضافة/إزالة المنتجات مع إدارة الكمية
- **الطلبات**: إدارة دورة الحياة الكاملة للطلب
- **لوحة التحكم للمسؤول**: إدارة المستخدمين والمنتجات والطلبات
- **التحكم في الوصول بناءً على الأدوار**: نقاط نهاية آمنة مع أدوار مستخدم/مسؤول
- **نظام تسجيل شامل**: تتبع جميع الإجراءات للتصحيح والمراقبة

---

## 🏗️ المعمارية

### نمط المعمارية: **المعمارية الموزونة (الطبقات الثلاثة)**

```
┌─────────────────────────────────────────────────┐
│              طبقة مسارات الـ API                 │
│        (routes/*.js, routes/admin/*.js)         │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│          طبقة المتحكمات (Controllers)            │
│    (controllers/*.controller.js)                │
│  - التحقق من صحة الإدخال                       │
│  - تنسيق الطلب                                  │
│  - تنسيق الاستجابة                             │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│         طبقة الخدمات (Services Layer)           │
│       (services/*.service.js)                   │
│  - المنطق التجاري                              │
│  - تحويل البيانات                              │
│  - العمليات بين الكيانات المختلفة              │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│        طبقة النماذج (Mongoose Models)          │
│         (models/*.model.js)                     │
│  - تعريفات المخطط                              │
│  - التحقق من البيانات                          │
│  - استعلامات قاعدة البيانات                    │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│        قاعدة البيانات (MongoDB)                 │
└─────────────────────────────────────────────────┘
```

### لماذا هذه المعمارية؟

✅ **فصل الاهتمامات**: لكل طبقة مسؤولية واحدة  
✅ **القابلية للاختبار**: سهلة محاكاة الخدمات واختبار المتحكمات  
✅ **سهولة الصيانة**: هيكل واضح يجعل التنقل في الكود بسيطاً  
✅ **قابلية التوسع**: يمكن استخراج الخدمات كخدمات صغيرة إذا لزم الأمر  
✅ **إعادة استخدام الكود**: يمكن مشاركة الخدمات بين متحكمات مختلفة

---

## 💻 المكدس التقني

| الطبقة | التقنية | الإصدار |
|-------|---------|---------|
| **بيئة التشغيل** | Node.js | أحدث LTS |
| **الإطار العام** | Express.js | 5.2.1 |
| **قاعدة البيانات** | MongoDB | 9.1.5 (عبر Mongoose ODM) |
| **المصادقة** | JWT | 9.0.3 |
| **التشفير** | bcryptjs | 3.0.3 |
| **أدوات التطوير** | Nodemon | 3.1.11 |
| **إعدادات البيئة** | dotenv | 17.2.3 |

---

## 📁 هيكل المشروع

```
e-commerce-backend/
├── config/                          # ملفات الإعدادات
│   ├── configapp.js                # إعدادات مستوى التطبيق
│   └── db.js                       # إعداد اتصال MongoDB
│
├── middleware/                      # برمجيات Express الوسيطة
│   ├── asyncHandler.js             # تغليف الأخطاء غير المتزامنة
│   ├── verifyToken.js              # المصادقة عبر JWT
│   ├── restrictTo.js               # التفويض بناءً على الأدوار
│   ├── errorHandler.js             # معالج الأخطاء العام
│   ├── checkOrderOwnership.js       # التحقق من ملكية الطلب
│   ├── restrictOrderStatus.js       # التحقق من حالة الطلب
│   └── validateOrderStatusTransition.js
│
├── models/                          # مخططات Mongoose
│   ├── User.js                     # مخطط المستخدم
│   ├── product.model.js            # مخطط المنتج
│   ├── order.model.js              # مخطط الطلب
│   ├── category.model.js           # مخطط الفئة
│   └── Categories.js               # فئة (بديل)
│
├── controllers/                     # معالجات الطلبات
│   ├── auth.controller.js          # منطق المصادقة
│   ├── users.controller.js         # عمليات CRUD للمستخدم
│   ├── products.controller.js      # عمليات CRUD للمنتج
│   ├── orders.controller.js        # إدارة الطلبات
│   ├── categories.controller.js    # إدارة الفئات
│   └── dashboard.controller.js     # لوحة معلومات المسؤول
│
├── services/                        # طبقة المنطق التجاري
│   ├── auth.service.js             # خدمة المصادقة
│   ├── users.service.js            # خدمة المستخدم
│   ├── products.service.js         # خدمة المنتج
│   ├── orders.service.js           # خدمة الطلب
│   ├── categories.service.js       # خدمة الفئة
│   └── dashboard.service.js        # تحليلات لوحة التحكم
│
├── routes/                          # نقاط نهاية الـ API
│   ├── auth.routes.js              # نقاط المصادقة
│   ├── users.routes.js             # نقاط المستخدم
│   ├── products.routes.js          # نقاط المنتج
│   ├── orders.routes.js            # نقاط الطلب
│   ├── categories.routes.js        # نقاط الفئة
│   ├── home.routes.js              # نقاط الصفحة الرئيسية
│   ├── notFound.routes.js          # معالج 404
│   └── admin/                      # مسارات المسؤول (محمية)
│       ├── index.js                # تجميع مسارات المسؤول
│       ├── users.route.js          # إدارة مستخدمي المسؤول
│       ├── products.route.js       # إدارة منتجات المسؤول
│       ├── categories.route.js     # إدارة فئات المسؤول
│       └── orders.route.js         # إدارة طلبات المسؤول
│
├── utils/                           # وظائف ودوال مساعدة
│   ├── AppError.js                 # فئة الخطأ المخصصة
│   ├── baseController.js           # فئة المتحكم الأساسي
│   ├── sendResponse.js             # منسق الاستجابة الموحد
│   ├── logger.js                   # أداة التسجيل
│   ├── apiFeatures.js              # البحث والتصفية والتصفح
│   ├── calculateOrderTotal.js      # دالة مساعدة لحساب الطلب
│   ├── CategoryFactory.js          # نمط مصنع الفئة
│   ├── generateSlug.js             # مولد اختصار URL
│   ├── pagination.js               # دالة مساعدة للتصفح
│   ├── roles.js                    # تعريفات الأدوار
│   ├── DSA/                        # هياكل بيانات وخوارزميات (للتعلم)
│   └── services/                   # خدمات أداة مساعدة إضافية
│
├── validators/                      # التحقق من صحة الإدخال
│   ├── auth.validator.js           # التحقق من إدخال المصادقة
│   ├── products.validator.js       # التحقق من إدخال المنتج
│   ├── category.validator.js       # التحقق من إدخال الفئة
│   └── validateId.js               # التحقق من معرف MongoDB
│
├── server.js                        # نقطة دخول التطبيق
├── package.json                     # المعتمديات والبرامج النصية
└── README.md                        # التوثيق

---

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية

- Node.js (الإصدار 14 أو أعلى)
- MongoDB (محلي أو Atlas)
- npm أو yarn

### الخطوة 1: استنساخ وتثبيت المعتمديات

```bash
git clone <رابط-المستودع>
cd e-commerce-backend
npm install
```

### الخطوة 2: إعداد متغيرات البيئة

أنشئ ملف `.env` في الدليل الجذر:

```env
# إعدادات الخادم
PORT=3000
NODE_ENV=development

# قاعدة البيانات
MONGODB_URI=mongodb://localhost:27017/e-commerce-backend

# المصادقة
JWT_SECRET=مفتاح_jwt_سري_قوي_جداً_هنا
JWT_EXPIRE=7d

# معالج السجلات
LOG_LEVEL=info
```

### الخطوة 3: بدء الخادم

```bash
npm start
```

سيكون API متاحاً على `http://localhost:3000`

---

## 📡 تدفق الطلب

### دورة الحياة الكاملة للطلب

```
طلب HTTP
    │
    ▼
┌─────────────────────────────────────┐
│   سلسلة البرمجيات الوسيطة Express    │
├─────────────────────────────────────┤
│ 1. express.json()                   │
│ 2. express.urlencoded()             │
│ 3. معالج السجلات للتطوير            │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   مطابقة المسار                      │
│   (routes/[feature].routes.js)      │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   برمجية المصادقة                    │
│   (إذا كان المسار محمياً)           │
│   - verifyToken() برمجية           │
│   - التحقق من توكن JWT             │
│   - جلب المستخدم من قاعدة البيانات │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   برمجية التفويض                    │
│   (إذا كان قائماً على الأدوار)    │
│   - restrictTo('admin', 'user')     │
│   - التحقق من دور المستخدم         │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   مدققات إضافية                    │
│   - validateId() - فحص معرف MongoDB │
│   - checkOrderOwnership()           │
│   - restrictOrderStatus()           │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   معالج المتحكم                     │
│   (controllers/*.controller.js)     │
│   - استقبال الطلب                  │
│   - التحقق من صحة الإدخال          │
│   - استدعاء طبقة الخدمات           │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   طبقة الخدمات                      │
│   (services/*.service.js)           │
│   - تنفيذ المنطق التجاري           │
│   - استعلامات نموذج Mongoose       │
│   - تحويل البيانات                 │
│   - عمليات بين الكيانات المختلفة   │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   قاعدة البيانات MongoDB            │
│   - تنفيذ الاستعلام               │
│   - التحقق من مخطط البيانات         │
│   - حفظ البيانات                   │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│   تنسيق الاستجابة                   │
│   أداة sendResponse()               │
│   - توحيد الاستجابة                │
│   - تعيين رمز الحالة               │
│   - تضمين البيانات الوصفية         │
└─────────────────────────────────────┘
    │
    ▼
استجابة HTTP (JSON)
```

### مثال: تدفق تسجيل الدخول للمستخدم

```
POST /api/auth/login
    │
    ▼
authRoutes.js (مطابقة المسار)
    │
    ▼
غلاف asyncHandler (معالجة الأخطاء)
    │
    ▼
authController.login()
    ├─ التحقق من صحة الإدخال
    │
    ▼
authService.login()
    ├─ البحث عن المستخدم بواسطة البريد الإلكتروني
    ├─ مقارنة كلمة المرور باستخدام bcrypt
    ├─ توليد توكن JWT
    │
    ▼
الاستجابة: { user, token }
```

---

## 🔌 نقاط نهاية الـ API

### نقاط المصادقة

```
POST   /api/auth/register         تسجيل مستخدم جديد
POST   /api/auth/login            تسجيل دخول المستخدم
```

### نقاط المستخدم

```
GET    /api/users/profile         الحصول على ملف المستخدم الحالي
PUT    /api/users/profile         تحديث ملف المستخدم
GET    /api/users/cart            الحصول على سلة التسوق
POST   /api/users/cart            إضافة عنصر للسلة
DELETE /api/users/cart/:id        إزالة من السلة
POST   /api/users/wishlist        إضافة للقائمة المفضلة
```

### نقاط المنتج

```
GET    /api/products              الحصول على جميع المنتجات
GET    /api/products/:id          الحصول على منتج حسب المعرف
POST   /api/products/search       البحث عن المنتجات (التصفية، التصفح)
```

### نقاط الفئة

```
GET    /api/categories            الحصول على جميع الفئات
GET    /api/categories/:id        الحصول على فئة حسب المعرف
```

### نقاط الطلب

```
POST   /api/orders                إنشاء طلب جديد
GET    /api/orders                الحصول على طلبات المستخدم
GET    /api/orders/:id            الحصول على تفاصيل الطلب
PUT    /api/orders/:id            تحديث حالة الطلب
```

### نقاط المسؤول

```
GET    /api/admin/user            إدارة المستخدمين
GET    /api/admin/product         إدارة المنتجات
GET    /api/admin/category        إدارة الفئات

DELETE /api/admin/user/:id        حذف مستخدم
PUT    /api/admin/product/:id     تحديث منتج
POST   /api/admin/category        إنشاء فئة
```

---

## 🔐 المصادقة والتفويض

### المصادقة القائمة على JWT

يستخدم النظام **رموز الويب JSON (JWT)** للمصادقة بدون حالة.

#### هيكل الرمز

```
الرأس: { alg: "HS256", typ: "JWT" }
الحمولة: { id: "معرف_المستخدم", role: "user", iat: 1234567890, exp: 1234654290 }
التوقيع: HMACSHA256(header + payload, JWT_SECRET)
```

#### التدفق

1. يرسل المستخدم بيانات الاعتماد إلى `/api/auth/login`
2. يتم التحقق من كلمة المرور باستخدام bcrypt
3. يتم توليد رمز JWT يحتوي على معرف المستخدم والدور
4. يتم إرسال الرمز للعميل (مخزن في localStorage/cookie)
5. يتضمن العميل الرمز في رأس التفويض: `Bearer <token>`
6. تتحقق برمجية `verifyToken` من الرمز على المسارات المحمية

#### مثال الحماية

```javascript
// مثال مسار محمي
router.get('/profile', 
    verifyToken,              // المصادقة
    restrictTo('user'),       // التفويض
    getProfile                // المعالج
);
```

### التحكم في الوصول بناءً على الأدوار (RBAC)

- **user**: عميل عادي (افتراضي)
- **admin**: يمكنه إدارة المنتجات والفئات والمستخدمين والطلبات

---

## 🎯 قرارات التصميم

### 1. **المعمارية الموزونة (نموذج الطبقات الثلاثة)**

**لماذا؟**
- معيار صناعي للتطبيقات القابلة للتوسع
- فصل واضح للاهتمامات (المنطق التجاري ≠ معالجة HTTP)
- اختبار أسهل (يمكن محاكاة الخدمات بشكل مستقل)
- جاهز للمستقبل للهجرة إلى الخدمات الصغيرة

**البديل المدروس:** MVC (Model-View-Controller)
- تم رفضه لأننا نحتاج إلى طبقة خدمات صريحة للمنطق التجاري

---

### 2. **فئة معالجة الأخطاء المخصصة (`AppError`)**

**لماذا؟**
```javascript
// بدلاً من:
res.status(401).json({ error: message })

// نستخدم:
throw new AppError(message, 401)
```

**الفوائد:**
- تنسيق خطأ موحد في جميع أنحاء الـ API
- تسجيل تلقائي مع السياق
- التمييز بين الأخطاء التشغيلية وأخطاء البرمجة
- هيكل استجابة خطأ موحد

---

### 3. **برمجية تغليف المعالجات غير المتزامنة**

**لماذا؟**
```javascript
// بدون asyncHandler - نحتاج try-catch في كل معالج
router.get('/', async (req, res, next) => {
    try {
        const data = await User.find();
        res.json(data);
    } catch (err) {
        next(err); // تمرير الخطأ يدويًا
    }
});

// مع asyncHandler - أنظف
router.get('/', asyncHandler(async (req, res) => {
    const data = await User.find();
    res.json(data);
}));
```

**الفوائد:**
- يمنع الأخطاء في الوعود غير المعالجة
- يلغي كود try-catch المتكرر
- يفرض معالجة الأخطاء من خلال البرمجيات الوسيطة

---

### 4. **فصل طبقة الخدمات**

**لماذا؟**
- تتعامل المتحكمات مع مخاوف HTTP (الطلب/الاستجابة)
- تتعامل الخدمات مع المنطق التجاري
- يمكن استخدام نفس الخدمة من قبل متحكمات متعددة
- من السهل إضافة التخزين المؤقت والتسجيل والعمليات على مستوى الخدمة

**مثال:**
```javascript
// الخدمة: تحتوي على المنطق التجاري
UserService.register = async (userData) => {
    const hashedPassword = await bcrypt.hash(userData.password, 10);
    return User.create({ ...userData, password: hashedPassword });
}

// المتحكم: خاص بـ HTTP
authController.register = async (req, res) => {
    const newUser = await authService.register(req.body);
    sendResponse(res, 201, newUser, "تم التسجيل بنجاح");
}
```

---

### 5. **فئة المتحكم الأساسي**

**لماذا؟**
```javascript
// تجنب التكرار في كل متحكم
class BaseController {
    send(res, statusCode, data, message) {
        sendResponse(res, statusCode, data, message);
    }
    logAction(action, details) {
        Logger.info(`${action} - ${details}`);
    }
}

// قم بتوسيعها
class AuthController extends BaseController {
    // ترث send() و logAction()
}
```

**الفوائد:**
- مبدأ DRY (عدم التكرار)
- تسجيل متسق عبر المتحكمات
- طرق مساعدة شائعة موروثة

---

### 6. **فهرس MongoDB على معرف المستخدم في الطلبات**

**لماذا؟**
```javascript
user: {
    type: mongoose.Schema.Types.ObjectId,
    ref: "User",
    required: true,
    index: true,  // ✅ فهرسة للبحث السريع
}
```

**الفوائد:**
- الاستعلامات مثل "احصل على جميع طلبات المستخدم" هي O(log n) بدلاً من O(n)
- حرج للأداء مع ملايين السجلات

---

### 7. **لقطة عنصر الطلب (الاسم، السعر)**

**لماذا؟** تخزين اسم المنتج والسعر في وقت الطلب
```javascript
items: [{
    product: ObjectId,      // رابط للمنتج
    name: String,           // لقطة من الاسم في وقت الطلب
    price: Number,          // لقطة من السعر في وقت الطلب
    quantity: Number,
    subtotal: Number
}]
```

**الفوائد:**
- دقة تاريخية (السعر لا يتغير إذا تم تحديث المنتج)
- يمكن حذف المنتج دون التأثير على سجل الطلبات السابقة
- يوفر مسار تدقيق تجاري

---

### 8. **نمط الحذف الناعم (علم isDeleted)**

**لماذا؟** بدلاً من حذف السجلات بقوة:
```javascript
// بدلاً من: await Product.deleteOne({ _id: id })
// نفعل:
await Product.updateOne({ _id: id }, { isDeleted: true })
```

**الفوائد:**
- احتمالية استرجاع البيانات
- مسار التدقيق (من حذف ومتى)
- لا توجد روابط كسر في المستندات الأخرى

---

### 9. **التصفح عبر `apiFeatures.js`**

**لماذا؟** لا تحمل جميع المنتجات مرة واحدة

```javascript
const products = await Product.find()
    .skip((page - 1) * pageSize)
    .limit(pageSize);
```

**الفوائد:**
- تقليل استخدام الذاكرة
- أوقات استجابة أسرع
- تجربة مستخدم أفضل

---

### 10. **Nodemon للتطوير**

**لماذا؟** إعادة تشغيل تلقائية عند تغيير الملفات

```json
{
    "scripts": {
        "start": "nodemon server.js"
    }
}
```

**الفوائد:**
- دورة تطوير أسرع (لا إعادة تشغيل يدوية)
- اكتشف أخطاء الصيغة فوراً

---

## ✨ الميزات الرئيسية

### 🔑 المصادقة الآمنة
- مصادقة قائمة على JWT بدون حالة
- تجزئة كلمات المرور باستخدام bcryptjs
- إدارة انتهاء صلاحية الرمز
- دعم رمز التحديث

### 🛡️ التحكم في الوصول بناءً على الأدوار
- أدوار المستخدم والمسؤول
- نقاط نهاية المسؤول المحمية
- التحكم الدقيق في الأذونات

### 🛒 ميزات التسوق
- إدارة سلة التسوق
- وظائف القائمة المفضلة
- إنشاء الطلب والتتبع
- دورة حياة حالة الطلب (قيد الانتظار → المعالجة → مشحونة → تم التسليم)

### 📊 لوحة تحكم المسؤول
- إدارة المستخدمين (عرض، حذف)
- إدارة المنتجات (CRUD)
- إدارة الفئات
- تتبع الطلبات وإدارتها

### 📝 التحقق من البيانات
- التحقق من صحة الإدخال على مستوى المتحكم
- التحقق من الشيم على مستوى النموذج
- مدققات مخصصة لتحولات حالة الطلب
- التحقق من معرف MongoDB

### 🚨 معالجة أخطاء شاملة
- فئة خطأ مخصصة مع التسجيل
- برمجية معالج الأخطاء العام
- استجابات أخطاء التطوير مقابل الإنتاج
- رسائل خطأ مفصلة في السجلات

### 📍 نظام التسجيل
- تسجيل الإجراء (من فعل ماذا)
- تسجيل الخطأ مع تتبع المكدس
- تسجيل الطلب في وضع التطوير

---

## 🗄️ مخطط قاعدة البيانات

### مجموعة المستخدمين

```json
{
  "_id": ObjectId,
  "name": String,
  "email": String (فريد),
  "password": String (مشفر),
  "role": String (افتراضي: "user"),
  "cart": [
    {
      "productId": ObjectId,
      "quantity": Number
    }
  ],
  "orders": [ObjectId],
  "wishlist": [
    {
      "productId": ObjectId
    }
  ],
  "createdAt": Date,
  "updatedAt": Date
}
```

### مجموعة المنتجات

```json
{
  "_id": ObjectId,
  "name": String,
  "description": String,
  "image": String,
  "slug": String (فريد),
  "categoryId": ObjectId,
  "price": Number,
  "quantity": Number,
  "isDeleted": Boolean (افتراضي: false),
  "createdAt": Date,
  "updatedAt": Date
}
```

### مجموعة الطلبات

```json
{
  "_id": ObjectId,
  "user": ObjectId (فهرسة),
  "items": [
    {
      "product": ObjectId,
      "name": String,          // لقطة
      "price": Number,         // لقطة
      "quantity": Number,
      "subtotal": Number
    }
  ],
  "shippingAddress": {
    "city": String,
    "street": String,
    "building": String,
    "phone": String
  },
  "totalPrice": Number,
  "status": String (enum),
  "createdAt": Date,
  "updatedAt": Date
}
```

### مجموعة الفئات

```json
{
  "_id": ObjectId,
  "name": String,
  "description": String,
  "createdAt": Date,
  "updatedAt": Date
}
```

---

## ⚠️ معالجة الأخطاء

### تدفق معالج الأخطاء العام

```
رفع AppError / Error في الخدمة
    │
    ▼
asyncHandler يقبض على الخطأ
    │
    ▼
يمرر إلى next(err)
    │
    ▼
برمجية globalErrorHandler
    │
    ▼
┌─────────────────────────────────────┐
│ تصنيف الخطأ:                        │
│                                     │
│ التطوير:                           │
│ - مسار المكدس الكامل              │
│ - تفاصيل الخطأ                     │
│ - معلومات التصحيح                 │
│                                     │
│ الإنتاج:                           │
│ - رسالة خطأ آمنة                  │
│ - لا تفاصيل تقنية                 │
│ - أخطاء تشغيلية فقط               │
└─────────────────────────────────────┘
    │
    ▼
استجابة JSON للعميل
```

### مثال استجابة الخطأ

**التطوير:**
```json
{
  "status": "fail",
  "message": "البريد الإلكتروني موجود بالفعل",
  "error": {},
  "stack": "Error: البريد الإلكتروني موجود بالفعل\n at AuthService.register..."
}
```

**الإنتاج:**
```json
{
  "status": "fail",
  "message": "البريد الإلكتروني موجود بالفعل"
}
```

---

## 🔧 متغيرات البيئة

| المتغير | الوصف | الافتراضي | مثال |
|---------|-------|----------|------|
| `PORT` | منفذ الخادم | 3000 | 3000 |
| `NODE_ENV` | البيئة | development | development/production |
| `MONGODB_URI` | رابط قاعدة البيانات | localhost:27017 | mongodb+srv://user:pass@cluster |
| `JWT_SECRET` | مفتاح توقيع الرمز | - | your_secret_key_here |
| `JWT_EXPIRE` | انتهاء صلاحية الرمز | 7d | 7d, 24h, 1y |
| `LOG_LEVEL` | مستوى التسجيل | info | debug, info, warn, error |

---

## 🚦 التحسينات المستقبلية

- [ ] دمج الدفع (Stripe/PayPal)
- [ ] إشعارات البريد الإلكتروني (تأكيد الطلب، تحديثات الشحن)
- [ ] تقييمات ومراجعات المنتجات
- [ ] إدارة المخزون والتنبيهات
- [ ] البحث المتقدم باستخدام Elasticsearch
- [ ] توثيق الـ API باستخدام Swagger/OpenAPI
- [ ] اختبارات الوحدة والتكامل (Jest، Supertest)
- [ ] تحديد معدل الطلب وأمان الـ API (helmet، express-validator)
- [ ] طبقة التخزين المؤقت (Redis)
- [ ] قائمة انتظار الرسائل (Bull، RabbitMQ)
- [ ] واجهة GraphQL بجانب REST
- [ ] حاويات Docker

---

## 📚 موارد التعلم

يطبق هذا المشروع عدة مفاهيم مهمة:

- **أنماط التصميم**: Factory, Strategy, Observer
- **مبادئ SOLID**: Single Responsibility, Dependency Injection
- **الأمان**: JWT, bcrypt, CORS, تحديد معدل الطلب
- **قاعدة البيانات**: MongoDB, Mongoose, تصميم المخطط، الفهرسة
- **الكود النظيف**: تسمية معنى، DRY, معالجة الأخطاء

---

## 📄 الترخيص

ISC

---

## 👨‍💻 المؤلف

[اسمك]

---

## 🤝 المساهمة

نرحب بالمساهمات! اتبع هذه الخطوات:

1. انسخ المستودع (Fork)
2. أنشئ فرع الميزة (`git checkout -b feature/AmazingFeature`)
3. الالتزم بالتغييرات (`git commit -m 'Add AmazingFeature'`)
4. ادفع إلى الفرع (`git push origin feature/AmazingFeature`)
5. افتح طلب دمج (Pull Request)

---

## ❓ الدعم

للمشاكل أو الأسئلة، يرجى فتح قضية على GitHub.

---

**تم البناء بـ ❤️ | Node.js | Express | MongoDB**
